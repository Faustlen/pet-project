FROM openjdk:21-jdk-slim
WORKDIR /app
COPY /build/libs/priceHistory-0.0.1-SNAPSHOT.jar /app/PriceHistory.jar
EXPOSE 8081
ENTRYPOINT ["sh", "-c", "java -DDB_HOST=${DB_HOST} -DDB_PORT=${DB_PORT} -DDB_NAME=${DB_NAME} -DDB_USERNAME=${DB_USERNAME} -DDB_PASSWORD=${DB_PASSWORD} -DKAFKA_BOOTSTRAP_SERVERS=${KAFKA_BOOTSTRAP_SERVERS} -DKF_TOPIC=${KF_TOPIC} -jar PriceHistory.jar"]